# 第二阶段调整计划

## 调整时间：2025年1月8日 19:00
## 调整原因：终端A需要先完成部署问题，终端B提前开始

### 一、当前状态

- **终端A**：解决第一阶段部署问题（进行中）
- **终端B**：准备开始第二阶段开发（就绪）

### 二、调整后的工作安排

#### 终端B - 独立开始任务
1. **立即开始**：B2.1 点赞收藏功能（3天）
   - 可以先开发前端组件
   - 使用Mock数据进行测试
   - 准备好与后端API对接

2. **准备工作**：
   ```bash
   # 安装必要依赖
   npm install framer-motion
   npm install @heroicons/react  # 图标库
   
   # 创建组件结构
   mkdir -p components/interactions
   mkdir -p hooks
   mkdir -p lib/mock
   ```

3. **Mock数据结构**：
   ```typescript
   // lib/mock/interactions.ts
   export const mockInteractions = {
     likes: {
       count: 42,
       hasLiked: false
     },
     bookmarks: {
       count: 18,
       hasBookmarked: false
     }
   }
   ```

#### 终端A - 部署完成后加入
1. **当前任务**：解决部署问题
2. **完成后**：
   - 立即开始A2.1 数据库设计
   - 为终端B提供真实API接口
   - 协助终端B将Mock切换到真实数据

### 三、终端B独立开发指南

#### 3.1 点赞组件开发
```typescript
// components/interactions/LikeButton.tsx
interface LikeButtonProps {
  contentId: string
  contentType: 'post' | 'project' | 'book' | 'tool'
  initialCount?: number
  hasLiked?: boolean
}
```

#### 3.2 收藏组件开发
```typescript
// components/interactions/BookmarkButton.tsx
interface BookmarkButtonProps {
  contentId: string
  contentType: 'post' | 'project' | 'book' | 'tool'
  hasBookmarked?: boolean
}
```

#### 3.3 临时API接口设计
```typescript
// 点赞接口
POST /api/likes
{
  contentId: string
  contentType: string
  action: 'like' | 'unlike'
}

// 收藏接口
POST /api/bookmarks
{
  contentId: string
  contentType: string
  action: 'bookmark' | 'unbookmark'
}

// 获取交互状态
GET /api/interactions/{contentId}
```

### 四、协作要点

1. **代码隔离**：
   - 终端B的组件放在 `components/interactions/`
   - 使用Props接口，便于后续集成
   - 保持组件纯净，不依赖认证状态

2. **样式规范**：
   - 遵循现有Tailwind CSS规范
   - 参考现有组件的设计风格
   - 移动端优先设计

3. **动画效果**：
   - 点赞：心形放大弹跳效果
   - 收藏：书签填充效果
   - 使用Framer Motion实现

### 五、时间线调整

```
1月8-9日：终端B开发点赞收藏组件（Mock数据）
1月9-10日：终端A完成部署，开始数据库设计
1月10-11日：终端A提供API，终端B集成真实数据
1月11日后：两端并行开发各自任务
```

### 六、风险管理

1. **接口变更风险**：
   - 终端B使用接口适配层
   - 保持Mock和真实API结构一致

2. **集成风险**：
   - 预留1天集成时间
   - 编写集成测试用例

### 七、每日检查点

- [ ] 终端B：组件框架完成
- [ ] 终端B：动画效果实现
- [ ] 终端A：部署问题解决
- [ ] 终端A：数据库表创建
- [ ] 双方：API对接成功

---

**批准人**：终端C  
**生效时间**：立即